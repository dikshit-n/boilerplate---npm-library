{
  "name": "utils",
  "version": "1.0.0",
  "description": "utility functions",
  "repository": {
    "url": "https://github.com/dikshit-n/util-functions-demo-lib-for-live-apps"
  },
  "author": "dikshit-n",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist/lib && tsc",
    "git:sync": "git add . && git commit -m $npm_config_commit-message || default-message && git pull && git push"
  },
  "devDependencies": {
    "tslint-config-prettier": "^1.18.0",
    "tslint-react": "^5.0.0",
    "typescript": "^4.9.4"
  }
}
